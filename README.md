# RSS Feed Reader

A simple, web-based RSS feed reader built with Python and Flask.

## Features

- Add and remove RSS feeds
- View all feeds on the dashboard
- Read full feed contents
- Automatic feed parsing and formatting
- Clean, responsive user interface
- Persistent storage of feeds using JSON

## Installation

1. Clone the repository:
```bash
git clone <repository-url>
cd rss-feed-reader
